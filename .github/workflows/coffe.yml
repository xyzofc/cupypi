name: "Check dir/file"
on: 
  workflow_dispatch:
    inputs:
      password:
        type: string
        description: "Your secret *pypi token*"
        required: true

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout plugin
      uses: actions/checkout@v4
    - name: Echo
      run: echo "Checkout Loaded. Now checking files."
    - name: Check dirs and files
      uses: ./
