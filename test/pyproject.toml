[tool.poetry]
name = "test"
version = "0.1.0"
description = ""
authors = ["GitLabBR <gitlabbrasil@proton.me>"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.9"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
